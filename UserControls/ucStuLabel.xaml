<UserControl x:Class="Linka.ucStuLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="32" d:DesignWidth="400">
    <Grid Margin="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="txtKey" FontSize="14" FontWeight="Bold" TextAlignment="Right" Width="190" VerticalAlignment="Center" Margin="0,0,0,0" Grid.Column="0" Grid.Row="0">KEY</TextBlock>
        <Border Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="25" BorderThickness="2" BorderBrush="#FF0000AA" Background="#FFF0F0FF">
            <TextBlock x:Name="txtVal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="3">Val</TextBlock>
        </Border>
    </Grid>
</UserControl>
