<UserControl x:Class="Linka.ucRosters"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Name="txtLabel" FontSize="12" FontWeight="Bold" Background="Silver" Padding="3" Grid.Row="0"></TextBlock>
        <DataGrid AutoGenerateColumns="True" HorizontalAlignment="Stretch" Margin="0" Name="dataGrid1" VerticalAlignment="Stretch" SelectionMode="Single" Grid.Row="1" SelectionChanged="dataGrid1_SelectionChanged" />
    </Grid>
</UserControl>
